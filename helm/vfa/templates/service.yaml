apiVersion: v1
kind: Service
metadata:
  name: {{ include "vfa.fullname" . }}
  labels:
    {{- include "vfa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "vfa.selectorLabels" . | nindent 4 }}
