FROM quay.io/keycloak/keycloak:26.7.0

USER root

# Production image: include only the custom VFA theme.
COPY theme/vfa /opt/keycloak/themes/vfa

RUN chown -R keycloak:keycloak /opt/keycloak/themes/vfa

USER keycloak

# Keep default entrypoint/cmd from upstream image.
